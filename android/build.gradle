<<<<<<< HEAD
buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.google.gms:google-services:4.4.2'
        classpath 'com.android.tools.build:gradle:7.4.2' // Cambia la versión según sea necesario
    }
}

allprojects {
=======
// build.gradle en el nivel raíz del proyecto (android/build.gradle)

buildscript {
>>>>>>> 07bab4834e3c6e76dbd58b1da7c9ab684db15c9c
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.0.2' // Asegúrate de usar una versión compatible
    }
}

allprojects {
    repositories {
        google()
        mavenCentral() // Mantén mavenCentral en lugar de jcenter
    }
}

rootProject.buildDir = "../build"
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(":app")
}

// Tarea de limpieza
tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
